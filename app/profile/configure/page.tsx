import React from 'react'

// Same as set-up, minus the message that they need to configure before they can submit playlists
const Configure = () => {
  return (
    <p>Configure</p>
  )
}

export default Configure